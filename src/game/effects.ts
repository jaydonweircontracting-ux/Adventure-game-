import type { CombatDirection, CombatPoint } from './combat';
export function knockback(position: CombatPoint, direction: CombatDirection, amount: number): CombatPoint { return { x: position.x + (direction === 'right' ? amount : direction === 'left' ? -amount : 0), y: position.y + (direction === 'down' ? amount : direction === 'up' ? -amount : 0) }; }
export function screenShake(durationMs = 100): void { if (typeof document === 'undefined') return; const frame = document.querySelector('.game-frame'); frame?.classList.add('is-shaking'); window.setTimeout(() => frame?.classList.remove('is-shaking'), durationMs); }
export function playCombatSound(kind: 'shing' | 'baa', muted: boolean): void { if (muted || typeof window === 'undefined') return; try { const AudioContextClass = window.AudioContext || (window as typeof window & { webkitAudioContext?: typeof AudioContext }).webkitAudioContext; if (!AudioContextClass) return; const context = new AudioContextClass(); const oscillator = context.createOscillator(); const gain = context.createGain(); oscillator.type = kind === 'shing' ? 'triangle' : 'sawtooth'; oscillator.frequency.setValueAtTime(kind === 'shing' ? 720 : 180, context.currentTime); oscillator.frequency.exponentialRampToValueAtTime(kind === 'shing' ? 1100 : 95, context.currentTime + .1); gain.gain.setValueAtTime(.055, context.currentTime); gain.gain.exponentialRampToValueAtTime(.001, context.currentTime + .12); oscillator.connect(gain); gain.connect(context.destination); oscillator.start(); oscillator.stop(context.currentTime + .12); } catch { /* Audio can be blocked before a user gesture. */ } }
